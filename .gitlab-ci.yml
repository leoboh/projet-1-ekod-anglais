stages:
  - deploy

deploy-pages:
  stage: deploy
  script:
    - echo "Publishing Works On My Machine from public/"
  pages: true
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
  environment: production
